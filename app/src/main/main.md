# Electron 配置入口

## 目录结构

```

src/main
├── consts.ts   -- 常量定义
├── main.md     -- 文档
├── main.ts     -- 项目入口
├── menu.ts     -- 窗口顶部的菜单栏
├── preload.ts  -- 运行前需要加在的脚本
├── types.d.ts  -- 类型声明文件
└── util.ts     -- 工具类

```