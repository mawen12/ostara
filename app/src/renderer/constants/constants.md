# 应用常量

## 工程结构

```

src/renderer/constants/
├── constants.md        -- 文档
├── icons.ts            -- 应用图标，基于MUI
├── ids.ts              -- 应用ID
├── regex.ts            -- 应用常用的正则的表达式
└── ui.ts               -- 应用UI参数

```